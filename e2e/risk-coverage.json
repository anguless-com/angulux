{
    "agl-motion": {
        "via": "transitive",
        "renderedBy": "agl-overlay",
        "note": "agl-overlay is the only production template that renders the motion COMPONENT; agl-select and agl-multiselect render agl-overlay, so the 'select' and 'multiselect' scenarios construct and tear it down. Note agl-dialog reaches motion through the [aglMotion] DIRECTIVE, which is a different declaration and not in this set"
    },
    "[aglMenuItemContent]": {
        "via": "transitive",
        "renderedBy": "agl-menu",
        "note": "menu constructs one per item; the 'menu' scenario clicks an item and therefore goes through it"
    },
    "agl-tieredMenuSub": {
        "via": "transitive",
        "renderedBy": "agl-tieredmenu",
        "note": "each submenu level is one instance; the 'tieredmenu' scenario opens a branch and clicks a nested item"
    },
    "agl-selectItem": {
        "via": "transitive",
        "renderedBy": "agl-select",
        "note": "one per option in the overlay; the 'select' scenario opens the overlay and picks one"
    },
    "li[aglMultiSelectItem]": {
        "via": "transitive",
        "renderedBy": "agl-multiselect",
        "note": "one per option in the overlay; the 'multiselect' scenario selects two items"
    },
    "agl-cellEditor": {
        "via": "template",
        "note": "in the #sec-table-adv block; the 'table advanced' scenario clicks a cell to switch it from display to input mode"
    },
    "agl-columnFilter": {
        "via": "template",
        "note": "in the #sec-table-adv block; the 'table advanced' scenario types then presses Enter, taking the row count from 3 to 1"
    },
    "agl-columnFilterFormElement": {
        "via": "transitive",
        "renderedBy": "agl-columnFilter",
        "note": "the real filter input when display=row; it is the field the 'table advanced' scenario types into"
    },
    "agl-treeTable": {
        "via": "template",
        "note": "both the #sec-treetable and #sec-treetable-scroll blocks"
    },
    "[aglTreeTableBody]": {
        "via": "transitive",
        "renderedBy": "agl-treetable",
        "note": "treetable constructs it to render the table body; every visible row goes through it"
    },
    "agl-treeTableToggler": {
        "via": "template",
        "note": "the expand/collapse control; the 'treetable' scenario clicks it, taking 1 row to 3. It is NOT rendered by agl-treetable — treetable projects a caller-supplied body template, and apps/verify/src/app.ts puts the toggler in that template itself"
    },
    "agl-treeTableCellEditor": {
        "via": "template",
        "note": "in the #sec-treetable-scroll block; the 'treetable scroll + cell edit' scenario clicks a cell and edits the value"
    },
    "[ttScrollableView]": {
        "via": "transitive",
        "renderedBy": "agl-treetable when scrollable=true",
        "note": "the #sec-treetable-scroll block enables scrollable; the scenario asserts .p-treetable-scrollable-view exists"
    }
}
